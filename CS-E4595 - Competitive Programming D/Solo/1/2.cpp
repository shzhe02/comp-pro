#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<int> vi;

#define PB push_back
#define SQ(a) (a) * (a)


int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  
  int a, b;
  cin >> a >> b;
  if (a == b) {
    cout << "Maija";
  } else {
    cout << "Uolevi";
  }
}
