#include <bits/stdc++.h>
#include <vector>

using namespace std;

int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);
  int n, k;
  cin >> n >> k;
  vector<pair<int, int>> smarts(n);
  vector<int> out(n);
  for (int i = 0; i < n; ++i) {:
  sort(smarts.begin(), smarts.end());
  int groupSize = n / k;
  int groupNum = 1;
  for (int i = 0; i < groupSize; ++i) {
    
  }
}
