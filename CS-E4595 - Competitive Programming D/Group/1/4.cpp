#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<int> vi;

#define PB push_back
#define SQ(a) (a) * (a)


int main() {
  ios::sync_with_stdio(0);
  cin.tie(0);

  for (int i = 0; i < 10; ++i) {
    cout << SQ(i) << '\n';
  }
  
}
